buildscript {
    repositories {
        google()
        mavenCentral()
    }

    ext{
        roomVersion='2.4.0'
        navigationVersion='1.2.0'
        coroutineVersion='1.5.2'
    }

    dependencies {
        classpath "com.android.tools.build:gradle:7.0.4"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.6.10"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}